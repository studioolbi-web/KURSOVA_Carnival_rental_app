package db.migration;

public class FlywayMarker {
    // Empty class to force Java Platform Module System (JPMS)
    // to recognize the db.migration package so it can be opened
    // in module-info.java for Flyway resource scanning.
}
